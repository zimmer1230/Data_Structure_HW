#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <stdint.h>
#include <math.h>
int main(){

    int n,m,input;
    scanf("%d%d%d",&input,&m,&n);//skip scan p
    int prnd=3;
    bool* bucket=(bool*)calloc(m,sizeof(bool));
    while(n--){
        scanf("%d",&input);
        printf("%d ",input);

        s = k*prnd;
        x = s%
        h(k) = floor(m*x)



        if(bucket[input]==1)
            printf("0\n");
        else{
            printf("1\n");
            bucket[input]=1;
        }

    }


}

